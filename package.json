{
  "name": "quizmaster-app",
  "version": "1.0.0",
  "description": "Modern full-stack quiz application with FastAPI backend and React frontend",
  "scripts": {
    "dev:backend": "./start_backend.sh",
    "dev:frontend": "./start_frontend.sh",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "setup": "cd backend && python3 sample_data.py"
  },
  "keywords": ["quiz", "react", "fastapi", "education", "learning"],
  "author": "QuizMaster Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
